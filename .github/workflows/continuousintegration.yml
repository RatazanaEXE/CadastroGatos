name: Meu Primeiro Workflow
run-name: Olá, meu nome é ${{ github.actor }} e estou rodando esse workflow automaticamente
on: [push]
jobs:
  # job ou uma action
  minha-primeira-action:
    # em qual sistema operacional vai rodar
    runs-on: ubuntu-latest
    # aqui são os steps da job
    steps:
      - run: echo "Olá, essa é minha primeira action"